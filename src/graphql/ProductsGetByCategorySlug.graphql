query ProductsGetByCategorySlug($slug: String!) {
	category(slug: $slug) {
		products {
			id
			images {
				alt
				url
			}
			name
			price
			slug
			categories {
				name
				slug
				id
			}
			rating
		}
		name
		description
		slug
	}
}
